import { css } from '@emotion/react';

const select = css`
 border-radius: 10px;
  height: 36px;
  font-family: 'Montserrat', sans-serif;
  font-size: 16px;
  font-weight: 600;
  outline: none;
`;

export const cls = {
  select,
};
