import { css } from '@emotion/react';

const container = css`
  grid-area: c;
  display: flex;
  flex-direction: column;
  align-items: center;
  background: #ffffff;
  border-radius: 10px;
  box-shadow: 0 0 2px rgba(0, 0, 0, 0.1);
`;

export const cls = {
  container,
};
