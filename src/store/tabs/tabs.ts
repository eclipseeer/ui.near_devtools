import { state } from './state';
import { actions } from './actions';

export const tabs = {
  state,
  // persistent: {
  //   localStorage: {
  //     exclude: ['runtimeData'],
  //   },
  // },
  actions,
};
